environments:
  fastapi_tts:
    # Assuming unpacked_envs and projects are subdirs next to api_manager.py
    env_path: "./index-tts/tts_runtime"  # Relative path
    project_root: "./index-tts" # Relative path
    script: "fastapi_demo.py"               # Script relative to project_root
    command_args: []
  # livetalking_onnx:
  #   env_path: "./livetalking-onnx/stream_runtime"
  #   project_root: "./livetalking-onnx"
  #   script: "app.py"
  #   # Base command args
  #   command_args:
  #     - "--transport"
  #     - "webrtc"
  #     - "--model"
  #     - "ultralight"
    # Default avatar/port handled by API logic, not duplicated here
  ditto:
    # Conda environment path - absolute path to the conda env directory
    env_path: "./ditto-talkinghead-windows/ditto_runtime"
    # Project root for the ditto-talkinghead-windows repo
    # Use absolute path for clarity, or "." if manage_api_test1.py is in the same directory
    project_root: "./ditto-talkinghead-windows"
    # Main entry script for ditto; adjust if you use another script (e.g. inference.py / webrtc.py)
    script: "app.py"
    command_args:
      - "--transport"
      - "webrtc"
      # --listenport will be added dynamically by the API manager

defaults:
  # livetalking_onnx:
  #   avatar_id: "nc"
  #   listen_port: "8000"
  fastapi_tts:
    port: "50000"
  ditto:
    avatar_id: "nc" 
    listen_port: "8200"